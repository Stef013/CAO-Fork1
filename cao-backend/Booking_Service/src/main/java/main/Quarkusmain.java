package main;
import static java.lang.Thread.sleep;
import io.quarkus.runtime.annotations.QuarkusMain;

//@QuarkusMain
//public class Quarkusmain implements Runnable {
    /*private Thread t;
    private String threadName;

    public Quarkusmain(String name) throws InterruptedException
    {
        //Calling sleep otherwise the threads work to fast and the information wont be done for next phase
        sleep(200);
        threadName = name;
        System.out.println("Creating " + threadName);
    }

    public void run()
    {
        //Creates blacklister service
        quarkusUtil a = new quarkusUtil();
        System.out.println("Running main method");
        io.quarkus.runtime.Quarkus.run();
    }

    public quarkusUtil start()
    {
        //Starting blacklist service
        System.out.println("Starting " +  threadName );
        if (t == null) {
            t = new Thread (this, threadName);
            t.start ();
        }
        return blcklstr;
    }*/
//}
